# 실제 값을 채운 뒤 이 파일을 .env로 복사해서 쓴다 (.env는 .gitignore에 있음).
OLLAMA_API_KEY=
# TTS: CLOVA Voice 계정 활성화 문제로 OpenAI TTS로 급전환(main.py 참고).
# NCP_CLOVA_VOICE_CLIENT_ID/SECRET은 계정 문제 풀리면 다시 쓸 수 있어 남겨둠.
OPENAI_API_KEY=
NCP_CLOVA_VOICE_CLIENT_ID=
NCP_CLOVA_VOICE_CLIENT_SECRET=
